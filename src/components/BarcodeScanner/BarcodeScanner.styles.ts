import {StyleSheet} from 'react-native';

export const styles = StyleSheet.create({
  barcodeButtonWrapper: {
    padding: 5,
    position: 'absolute',
  },
  barcodeIconWrapper: {
    marginTop: 1,
    marginRight: 4,
  },
});
