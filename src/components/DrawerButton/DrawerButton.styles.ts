import {StyleSheet} from 'react-native';

export const styles = StyleSheet.create({
  buttonStyle: {
    padding: 10,
    marginRight: 10,
    marginLeft: -10,
    justifyContent: 'center',
    alignItems: 'center',
  },
  iconStyle: {
    fontSize: 30,
    color: '#fff',
    alignItems: 'center',
    justifyContent: 'center',
  },
});
