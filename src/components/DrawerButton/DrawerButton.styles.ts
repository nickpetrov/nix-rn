import {StyleSheet} from 'react-native';

export const styles = StyleSheet.create({
  buttonStyle: {
    width: 55,
    justifyContent: 'center',
    alignItems: 'center',
    paddingRight: 20,
  },
  iconStyle: {
    width: 25,
    fontSize: 25,
    color: '#fff',
    alignItems: 'center',
    justifyContent: 'center',
  },
});
