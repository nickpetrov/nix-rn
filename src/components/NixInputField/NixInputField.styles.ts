import {StyleSheet} from 'react-native';

export const styles = StyleSheet.create({
  errorText: {
    fontSize: 10,
    color: 'red',
  },
  errorInput: {
    borderColor: 'red',
  },
  inputWrapper: {
    marginVertical: 10,
    width: '100%',
  },
});
