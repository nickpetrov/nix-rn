import {StyleSheet} from 'react-native';

export const styles = StyleSheet.create({
  root: {
    marginHorizontal: 10,
    borderWidth: 1,
    borderColor: '#666',
    borderRadius: 10,
  },
  title: {
    textAlign: 'center',
    fontSize: 18,
    fontWeight: 'bold',
    padding: 10,
  },
});
