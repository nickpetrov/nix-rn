import {StyleSheet} from 'react-native';

export const styles = StyleSheet.create({
  titleWrapper: {
    borderBottomColor: '#bebebe',
    borderBottomWidth: 1,
  },
  title: {
    backgroundColor: '#fff',
    padding: 8,
    textAlign: 'center',
  },
});
