import {StyleSheet} from 'react-native';

export const styles = StyleSheet.create({
  cameraPopupWrapper: {
    flex: 1,
    backgroundColor: '#fff',
  },
  cameraPopup: {},
  photoPopup: {},
  buttonClose: {
    marginTop: 30,
  },
  camera: {
    display: 'flex',
    height: 300,
    width: 300,
    alignSelf: 'center',
    marginTop: 100,
  },
});
