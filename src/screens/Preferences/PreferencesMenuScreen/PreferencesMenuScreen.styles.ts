import {StyleSheet} from 'react-native';

export const styles = StyleSheet.create({
  menuItem: {
    borderBottomColor: '#bebebe',
    borderBottomWidth: 1,
    padding: 12,
    backgroundColor: '#fff',
  },
});
